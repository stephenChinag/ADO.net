using System;

class Program
{
    static void CalculateSum(int a, int b  )
    {
        int sum = a + b; // Must assign a value
        Console.WriteLine(sum);
    }

    static void Main()
    {
   
        CalculateSum(3, 7  );
        // Console.WriteLine(result); // Output: 10
    }
}
